- name: Get latest version from github
  github.project.version:
    name:    "{{ storage1_github_project_name    | default(storage1_releases_url) }}"
    version: "{{ storage1_github_project_version | default(storage1_release       | default('')) }}"
  register: result

- name: Dump result
  debug:
    msg: '{{ result }}'
    verbosity: 2

- name: Dump fact github_project_version 
  debug:
    msg: '{{ github_project_version }}'
    verbosity: 1
