#- name: storage1_helm_repo_name
#  debug:
#    msg: 'storage1_helm_repo_name: {{ storage1_helm_repo_name }}'
#    verbosity: 2
#
#- name: storage1_helm_repo_url
#  debug:
#    msg: 'storage1_helm_repo_url: {{ storage1_helm_repo_url }}'
#    verbosity: 2
#
#- name: storage1_helm_chart_name
#  debug:
#    msg: 'storage1_helm_chart_name: {{ storage1_helm_chart_name }}'
#    verbosity: 2
#
#- name: storage1_helm_chart_version
#  debug:
#    msg: 'storage1_helm_chart_version: {{ storage1_helm_chart_version}}'
#    verbosity: 2

- name: get latest helm chart version
  helm.chart.version:
    repo_name:  "{{ storage1_helm_repo_name }}"
    repo_url:   "{{ storage1_helm_repo_url }}"
    name:       "{{ storage1_helm_chart_name }}"
    version:    "{{ storage1_helm_chart_version | default('') }}"
  register: result

- name: dump result
  debug:
    msg: '{{ result }}'
    verbosity: 2

- name: dump fact helm_chart_version
  debug:
    msg: '{{ helm_chart_version }}'
    verbosity: 1
