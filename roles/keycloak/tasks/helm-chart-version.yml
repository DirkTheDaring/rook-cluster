# - name: keycloak_helm_repo_name
#   debug:
#     msg: 'keycloak_helm_repo_name: {{ keycloak_helm_repo_name }}'
#     verbosity: 2
#
# - name: keycloak_helm_repo_url
#   debug:
#     msg: 'keycloak_helm_repo_url: {{ keycloak_helm_repo_url }}'
#     verbosity: 2
#
# - name: keycloak_helm_chart_name
#   debug:
#     msg: 'keycloak_helm_chart_name: {{ keycloak_helm_chart_name }}'
#     verbosity: 2
#
# - name: keycloak_helm_chart_version
#   debug:
#     msg: 'keycloak_helm_chart_version: {{ keycloak_helm_chart_version}}'
#     verbosity: 2

- name: Get latest helm chart version
  helm.chart.version:
    repo_name:  "{{ keycloak_helm_repo_name }}"
    repo_url:   "{{ keycloak_helm_repo_url }}"
    name:       "{{ keycloak_helm_chart_name }}"
    version:    "{{ keycloak_helm_chart_version | default('') }}"
  register: result

- name: Dump result
  ansible.builtin.debug:
    msg: '{{ result }}'
    verbosity: 2

- name: Dump fact helm_chart_version
  ansible.builtin.debug:
    msg: '{{ helm_chart_version }}'
    verbosity: 1
