- name: deploy with helm
  block:
  - name: Add chart repo
    kubernetes.core.helm_repository:
      name:     "{{ rook_helm_repo_name }}"
      repo_url: "{{ rook_helm_repo_url }}"


  - name: render values.yaml
    set_fact:
      rook_values_yaml: "{{ lookup('template', 'values.yaml.jinja2') }}"


  - name: Deploy helm chart
    kubernetes.core.helm:
      chart_ref:         "{{ rook_helm_repo_name }}/{{ rook_helm_chart_name }}"
      chart_version:     "{{ rook_helm_chart_version }}"
      release_namespace: "{{ rook_namespace }}"
      release_name:      "{{ rook_chart_release_name | default('rook-ceph') }}"
      create_namespace:  true
#      values:            "{{ rook_values_yaml | from_yaml }}"
      update_repo_cache: yes

  when: rook_deployment == "helm"
