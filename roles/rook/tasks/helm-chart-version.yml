# - name: rook_helm_repo_name
#   debug:
#     msg: 'rook_helm_repo_name: {{ rook_helm_repo_name }}'
#     verbosity: 2
#
# - name: rook_helm_repo_url
#   debug:
#     msg: 'rook_helm_repo_url: {{ rook_helm_repo_url }}'
#     verbosity: 2
#
# - name: rook_helm_chart_name
#   debug:
#     msg: 'rook_helm_chart_name: {{ rook_helm_chart_name }}'
#     verbosity: 2
#
# - name: rook_helm_chart_version
#   debug:
#     msg: 'rook_helm_chart_version: {{ rook_helm_chart_version}}'
#     verbosity: 2

- name: Get latest helm chart version
  helm.chart.version:
    repo_name:  "{{ rook_helm_repo_name }}"
    repo_url:   "{{ rook_helm_repo_url }}"
    name:       "{{ rook_helm_chart_name }}"
    version:    "{{ rook_helm_chart_version | default('') }}"
  register: result

- name: Dump result
  ansible.builtin.debug:
    msg: '{{ result }}'
    verbosity: 2

- name: Dump fact helm_chart_version
  ansible.builtin.debug:
    msg: '{{ helm_chart_version }}'
    verbosity: 1
