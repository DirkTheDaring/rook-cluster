- name: Get latest version from github
  github.project.version:
    name:    "{{ vault_github_project_name    | default(vault_releases_url) }}"
    version: "{{ vault_github_project_version | default(vault_release       | default('')) }}"
  register: result

- name: Dump result
  debug:
    msg: '{{ result }}'
    verbosity: 2

- name: Dump fact github_project_version 
  debug:
    msg: '{{ github_project_version }}'
    verbosity: 1
