# fluxcd_metadata_url: https://api.github.com/repos/{{ fluxcd_github_repo }}/releases/{{ fluxcd_suffix_url }}
fluxcd_bin_dir:                "{{ ansible_user_dir | default('/usr/local') + '/bin' }}"

fluxcd_github_project_name:    fluxcd/flux2
# fluxcd_github_project_version: "v0.38.2"
fluxcd_github_project_version: ""

fluxcd_download_dir: "{{ playbook_dir }}/downloads"
fluxcd_path: "clusters/production"
