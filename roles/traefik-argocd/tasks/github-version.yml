- name: Get latest version from github
  github.project.version:
    name:    "{{ traefik_github_project_name    | default(traefik_releases_url) }}"
    version: "{{ traefik_github_project_version | default(traefik_release       | default('')) }}"
  register: result

- name: Dump result
  debug:
    msg: '{{ result }}'
    verbosity: 2

- name: Dump fact github_project_version 
  debug:
    msg: '{{ github_project_version }}'
    verbosity: 1
