kwhoami_credentials_dir: "{{ kwhoami_root_credentials_dir }}/kwhoami"

kwhoami_fqdn:                  "{{ kwhoami_hostname }}{{ '' if kwhoami_subdomain == '' else '.' + kwhoami_subdomain }}.{{ kwhoami_domain }}"
