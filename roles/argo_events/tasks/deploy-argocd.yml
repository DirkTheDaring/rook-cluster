- name: deploy argocd app
  block:
  - name: Transform from helm values to application params
    set_fact:
      argo_events_application_params: "{{ argo_events_values | chart_values_to_argo_parameters(argo_events_force_string_dict) }}"
  
#  - name: Write result to /tmp/argo-events-application.yaml
#    ansible.builtin.copy:
#      content: "{{ argo_events_application_params }}"
#      dest: /tmp/argo-events-application-params.yaml
#    when: ansible_verbosity > 2
  
  - name: Create /tmp/argo-events-application.yaml if verbosity > 2
    ansible.builtin.template:
      src:  "application.yaml.jinja2"
      dest: "/tmp/argo-events-application.yaml"
      mode: '0664'
    when: ansible_verbosity > 2
  
  - name: Create argocd application
    kubernetes.core.k8s:
      template: application.yaml.jinja2

  when: argo_events_deployment == "argocd"
