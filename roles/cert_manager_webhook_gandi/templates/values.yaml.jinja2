# -- Gandi Secret API key
# -- To not store it in plain text, use sops or similar.
# -- The secret is not created if not set.
gandiApiToken: "{{ cert_manager_webhook_gandi_api_token }}"

certManager:
  namespace: {{ cert_manager_webhook_gandi_cert_manager_namespace }}
  serviceAccountName: {{ cert_manager_webhook_gandi_cert_manager_service_acount_name }}
