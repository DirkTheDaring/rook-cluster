apiVersion: v1
kind: Service
metadata:
  namespace: {{ traefik_forward_auth_namespace }}
  name:      {{ config.name }}-oauth
spec:
  ports:
  - name:       http
    port:       4181
    protocol:   TCP
    targetPort: 4181
  selector:
    app: {{ config.name }}
