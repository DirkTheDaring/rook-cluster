- set_fact:
    whoami_file_list:
    - namespace.yaml.jinja2
    - deployment.yaml.jinja2
    - service.yaml.jinja2
    - ingress.yaml.jinja2

- name: apply operator
  kubernetes.core.k8s:
    template: "{{ item }}"
  loop: "{{ whoami_file_list }}"
