- ansible.builtin.template:
    src:  application.yaml.jinja2
    dest: /tmp/application.yaml
    mode: '0644'

#- set_fact:
#    harbor_application: "{{ lookup('template', 'application.yaml.jinja2')  }}"

#- name: show templating results
#  debug:
#    msg: "{{ harbor_application }}"
#    verbosity: 2

- name: create application
  kubernetes.core.k8s: 
    template: application.yaml.jinja2

