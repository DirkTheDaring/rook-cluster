mosquitto_credentials_dir: "{{ mosquitto_root_credentials_dir }}/mosquitto"

mosquitto_fqdn:                  "{{ mosquitto_hostname }}{{ '' if mosquitto_subdomain == '' else '.' + mosquitto_subdomain }}.{{ mosquitto_domain }}"
