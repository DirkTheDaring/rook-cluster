# - name: velero_helm_repo_name
#   debug:
#     msg: 'velero_helm_repo_name: {{ velero_helm_repo_name }}'
#     verbosity: 2
#
# - name: velero_helm_repo_url
#   debug:
#     msg: 'velero_helm_repo_url: {{ velero_helm_repo_url }}'
#     verbosity: 2
#
# - name: velero_helm_chart_name
#   debug:
#     msg: 'velero_helm_chart_name: {{ velero_helm_chart_name }}'
#     verbosity: 2
#
# - name: velero_helm_chart_version
#   debug:
#     msg: 'velero_helm_chart_version: {{ velero_helm_chart_version}}'
#     verbosity: 2

- name: Get latest helm chart version
  helm.chart.version:
    repo_name:  "{{ velero_helm_repo_name }}"
    repo_url:   "{{ velero_helm_repo_url }}"
    name:       "{{ velero_helm_chart_name }}"
    version:    "{{ velero_helm_chart_version | default('') }}"
  register: result

- name: Dump result
  ansible.builtin.debug:
    msg: '{{ result }}'
    verbosity: 2

- name: Dump fact helm_chart_version
  ansible.builtin.debug:
    msg: '{{ helm_chart_version }}'
    verbosity: 1
