- name: Get latest version from github
  github.project.version:
    name:    "{{ velero_github_project_name | default(velero_releases_url) }}"
    version: "{{ velero_github_project_version | default(velero_release | default('')) }}"
  register: result

- name: Dump result
  ansible.builtin.debug:
    msg: '{{ result }}'
    verbosity: 2

- name: Dump fact github_project_version
  ansible.builtin.debug:
    msg: '{{ github_project_version }}'
    verbosity: 1
