{
  "hostname": "{{ minio_api_fqdn }}",
  "url": "https://{{ minio_api_fqdn }}",
  "aws_access_key_id": "{{ minio_username }}",
  "aws_secret_access_key": "{{ minio_password }}",
  "buckets": {{ minio_bucket_names | to_json }}
}
